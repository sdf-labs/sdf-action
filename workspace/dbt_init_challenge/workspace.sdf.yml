workspace:
  edition: "1.2"
  name: dbt_hol
  includes:
    - path: target/compiled/sdf/models
      index: catalog-schema-table-name
  providers:
    - namespaces:
        - target: census.public.*
      type: snowflake
  defaults:
    environment: dbg
    dialect: snowflake
    catalog: "${{ env_var('CATALOG') }}"
    schema: "${{ env_var('SCHEMA') }}"
